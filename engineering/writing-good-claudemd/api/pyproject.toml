[project]
name = "writing-good-claudemd-api"
version = "0.1.0"
description = "FastAPI backend for the writing-good-claudemd stage."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.0",
]
