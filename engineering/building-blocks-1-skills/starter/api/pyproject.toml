[project]
name = "harness-eng-2-skills-api"
version = "0.1.0"
description = "FastAPI backend for the harness-eng-2-skills stage."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.0",
]
